// displays a button to the user
// when the button is clicked, the user is redirected to the /api route "/box"

document.addEventListener('DOMContentLoaded', () => {
    console.log('JavaScript is loaded from an external file!');
});